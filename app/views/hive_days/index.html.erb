<h1>Listing hive_days</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @hive_days.each do |hive_day| %>
  <tr>
    <td><%= link_to 'Show', hive_day %></td>
    <td><%= link_to 'Edit', edit_hive_day_path(hive_day) %></td>
    <td><%= link_to 'Destroy', hive_day, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Hive day', new_hive_day_path %>
